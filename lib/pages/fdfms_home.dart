import 'package:flutter/material.dart';

class FdfmsHome extends StatelessWidget {
  const FdfmsHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('FDFMS Home'),
      ),
    );
  }
}
